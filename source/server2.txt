// TODO: server code that manages the document and handles client instructions
#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <sys/stat.h>
#include <time.h>
#include <string.h>
#include <fcntl.h>
#include <stdlib.h>
//#ifndef _POSIX_C_SOURCE
#define _POSIX_C_SOURCE 200809L
//#endif
#include <signal.h>
#include <pthread.h>
#include "../libs/markdown.h"
//#include <asm-generic/siginfo.h>
//#include <asm-generic/signal.h>

#define SUCCESS 0
#define INVALID_CURSOR_POS -1
#define DELETED_POSITION -2
#define OUTDATED_VERSION -3
#define MAX_LEN 64

long int TIME_INTERVAL;
document* my_doc;
char* all_logs = NULL;
char* version_log = NULL;
//uint64_t version_number = 1;
pthread_mutex_t log_lock = PTHREAD_MUTEX_INITIALIZER;
pthread_mutex_t doc_lock = PTHREAD_MUTEX_INITIALIZER;
pthread_mutex_t client_lock = PTHREAD_MUTEX_INITIALIZER;
volatile bool terminate = false;
pthread_mutex_t term_lock = PTHREAD_MUTEX_INITIALIZER;

struct client_pipe {
    FILE* s2c_fd;
    struct client_pipe* next;
    struct client_pipe* prev;
};

struct all_clients {
    int num_clients;
    struct client_pipe* clients;
    //struct client_pipe* tail_clients;
};

struct all_clients* my_clients;

void append_to_log(char* line) {
    size_t old_len = strlen(version_log);
    size_t add_len = strlen(line);
    version_log = realloc(version_log, old_len+add_len+1);
    memcpy(version_log+old_len, line, add_len);
    version_log[old_len+add_len] = '\0';
    return;
}

void* broadcast(void*) {
    while (true) {
        pthread_mutex_lock(&term_lock);
        bool term = terminate;
        pthread_mutex_unlock(&term_lock);

        if (term) {
            return NULL;
        }
        usleep(TIME_INTERVAL * 1000);

        pthread_mutex_lock(&log_lock);

        bool has_success = false;

        char* save_ptr = NULL;
        char* temp = strdup(version_log);
        char* line = strtok_r(temp, "\n", &save_ptr);
        while (line) {
            if (strstr(line, "SUCCESS")) {
                has_success = true;
                break;
            }
            line = strtok_r(NULL, "\n", &save_ptr);
        }
        free(temp);

        if (has_success) {
            pthread_mutex_lock(&doc_lock);
            markdown_increment_version(my_doc);
            pthread_mutex_unlock(&doc_lock);
            //version_number++;
        }

        char version_header[64];
        snprintf(version_header, sizeof(version_header), "VERSION %lu\n", my_doc->version);
        size_t version_header_len = strlen(version_header);
        // version_log = realloc(version_log, strlen(version_header) + 1);
        // memcpy(version_log, version_header, strlen(version_header)+1);
        //strcpy(version_log, version_header);

        size_t version_log_len = strlen(version_log);
        char* end_marker = "END\n";
        size_t payload_len = version_header_len + version_log_len + strlen(end_marker) + 1;
        char* payload = malloc(payload_len);
        snprintf(payload, payload_len, "%s%s%s", version_header, version_log, end_marker);

        size_t all_logs_len = strlen(all_logs);
        all_logs = realloc(all_logs, all_logs_len + payload_len);
        memcpy(all_logs+all_logs_len, payload, payload_len);
        //all_logs[all_logs_len+payload_len-1] = '\0';
        //strcat(all_logs, payload);
        //free(payload);

        pthread_mutex_lock(&client_lock);
        struct client_pipe* current = my_clients->clients;
        while (current) {
            fprintf(current->s2c_fd, "%s", payload);
            fflush(current->s2c_fd);
            current = current->next;
        }
        pthread_mutex_unlock(&client_lock);
        free(payload);

        version_log = realloc(version_log, 1);
        version_log[0] = '\0';

        // if (has_success) {
        //     pthread_mutex_lock(&doc_lock);
        //     markdown_increment_version(my_doc);
        //     pthread_mutex_unlock(&doc_lock);
        //     //version_number++;
        // }

        // //int new_version = my_doc->version;
        // char version_header[64];
        // snprintf(version_header, sizeof(version_header), "VERSION %lu\n", my_doc->version/*version_number*/);
        // version_log = realloc(version_log, strlen(version_header) + 1);
        // memcpy(version_log, version_header, strlen(version_header)+1);
        // //strcpy(version_log, version_header);

        pthread_mutex_unlock(&log_lock);
    }
    return NULL;
}

void* client_thread(void* arg) {
    pid_t client_pid = *((pid_t*) arg);
    //printf("client pid: %d\n", client_pid);
    free(arg);
    char c2s_fifo[MAX_LEN];
    char s2c_fifo[MAX_LEN];
    snprintf(c2s_fifo, MAX_LEN, "FIFO_C2S_%d", client_pid);
    snprintf(s2c_fifo, MAX_LEN, "FIFO_S2C_%d", client_pid);
    unlink(c2s_fifo);
    unlink(s2c_fifo);
    mode_t perm = 0600;
    mkfifo(c2s_fifo, perm);
    mkfifo(s2c_fifo, perm);
    
    kill(client_pid, SIGRTMIN+1);
    
    FILE* fd_c2s = fopen(c2s_fifo, "r");
    FILE* fd_s2c = fopen(s2c_fifo, "w");

    char username[64];
    char* role = NULL;
    bool authorised = false;
    fgets(username, sizeof(username) - 1, fd_c2s);
    //username[r] = '\0';
    username[strcspn(username, "\n")] = '\0';
    //printf("user %s joined\n", username);
    FILE* roles = fopen("roles.txt", "r");
    char roles_line[256];
    bool found = false;
    while (fgets(roles_line, sizeof(roles_line), roles)) {
        roles_line[strcspn(roles_line, "\n")] = '\0';
        char* save_ptr = NULL;
        char* usr_token = strtok_r(roles_line, " \t", &save_ptr);
        if (usr_token && strcmp(usr_token, username) == 0) {
            found = true;
            role = strtok_r(NULL, " \t", &save_ptr);
            //printf("role found: %s\n", role);
            break;
        }
    }
    fclose(roles);
    if (!found) {
        fprintf(fd_s2c, "Reject UNAUTHORISED\n");
        fflush(fd_s2c);
        sleep(1);
        fclose(fd_c2s);
        fclose(fd_s2c);
        unlink(c2s_fifo);
        unlink(s2c_fifo);
        return NULL;
    }
    if (strcmp(role, "write") == 0) {
        authorised = true;
    }
    pthread_mutex_lock(&doc_lock);
    char* doc_content = markdown_flatten(my_doc);
    pthread_mutex_unlock(&doc_lock);
    size_t doc_len = strlen(doc_content);
    fprintf(fd_s2c, "%s\n%lu\n%lu\n%s", role, my_doc->version, doc_len, doc_content);
    fflush(fd_s2c);
    free(doc_content);

    pthread_mutex_lock(&client_lock);
    struct client_pipe* client = malloc(sizeof(struct client_pipe));
    client->s2c_fd = fd_s2c;
    client->next = NULL;
    client->prev = NULL;
    if (!my_clients->clients) {
        my_clients->clients = client;
        my_clients->num_clients++;
    }
    else {
        client->next = my_clients->clients;
        my_clients->clients->prev = client;
        my_clients->clients = client;
        my_clients->num_clients++;
    }
    pthread_mutex_unlock(&client_lock);

    char command_summary[512];
    char command_buf[256+1];
    char command_buf_copy[256+1];
    //printf("reached here\n");
    while (fgets(command_buf, 256, fd_c2s)) {
        snprintf(command_buf_copy, 256+1, "%s", command_buf);
        command_buf[strcspn(command_buf, "\n")] = '\0';
        command_buf_copy[strcspn(command_buf_copy, "\n")] = '\0';
        char* saveptr;
        char* cmd = strtok_r(command_buf_copy, " ", &saveptr);
        //if (!cmd) continue;

        if (strcmp(cmd, "DISCONNECT") != 0) {
            if (!authorised) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject UNAUTHORISED\n", username, command_buf);
                pthread_mutex_lock(&log_lock);
                append_to_log(command_summary);
                pthread_mutex_unlock(&log_lock);
                continue;
            }
        }
        else {
            break;
        }

        if (strcmp(cmd, "INSERT") == 0) {
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            char* content = strtok_r(NULL, "", &saveptr);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_insert(my_doc, my_doc->version, pos, content);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "DEL") == 0) {
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            size_t len = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_delete(my_doc, my_doc->version, pos, len);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "NEWLINE") == 0) {
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_newline(my_doc, my_doc->version, pos);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "HEADING") == 0) {
            size_t level = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_heading(my_doc, my_doc->version, level, pos);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "BOLD") == 0) {
            size_t start = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            size_t end = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_bold(my_doc, my_doc->version, start, end);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "ITALIC") == 0) {
            size_t start = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            size_t end = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_italic(my_doc, my_doc->version, start, end);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "BLOCKQUOTE") == 0) {
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_blockquote(my_doc, my_doc->version, pos);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "ORDERED_LIST") == 0) {
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_ordered_list(my_doc, my_doc->version, pos);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "UNORDERED_LIST") == 0) {
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_unordered_list(my_doc, my_doc->version, pos);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "CODE") == 0) {
            size_t start = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            size_t end = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_code(my_doc, my_doc->version, start, end);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "HORIZONTAL_RULE") == 0) {
            size_t pos = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_horizontal_rule(my_doc, my_doc->version, pos);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        else if (strcmp(cmd, "LINK") == 0) {
            size_t start = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            size_t end = (size_t) strtol(strtok_r(NULL, " ", &saveptr), NULL, 10);
            char* url = strtok_r(NULL, "", &saveptr);
            pthread_mutex_lock(&doc_lock);
            int ret_val = markdown_link(my_doc, my_doc->version, start, end, url);
            pthread_mutex_unlock(&doc_lock);

            char* reason = NULL;
            if (ret_val == SUCCESS) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s SUCCESS\n", username, command_buf);
            }
            // else {
            //     snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %d\n", username, command_buf, ret_val);
            // }
            else if (ret_val == INVALID_CURSOR_POS) {
                reason = "INVALID_POSITION";
            }
            else if (ret_val == DELETED_POSITION) {
                reason = "DELETED_POSITION";
            }
            else if (ret_val == OUTDATED_VERSION) {
                reason = "OUTDATED_VERSION";
            }
            if (reason) {
                snprintf(command_summary, sizeof(command_summary), "EDIT %s %s Reject %s\n", username, command_buf, reason);
            }
            pthread_mutex_lock(&log_lock);
            append_to_log(command_summary);
            pthread_mutex_unlock(&log_lock);
        }
        // else if (strcmp(cmd, "DISCONNECT") == 0) {
        //     break;
        // }
    }
    pthread_mutex_lock(&client_lock);
    if (client->prev) {
        client->prev->next = client->next;
    }
    if (client->next) {
        client->next->prev = client->prev;
    }
    if (my_clients->clients == client) {
        my_clients->clients = client->next;
    }
    my_clients->num_clients--;
    free(client);
    client = NULL;
    pthread_mutex_unlock(&client_lock);

    fclose(fd_c2s);
    fclose(fd_s2c);
    unlink(c2s_fifo);
    unlink(s2c_fifo);
    return NULL;
}

void sig_handler(int sig, siginfo_t* info, void*) {
    if (sig == SIGRTMIN) {
        pid_t client_pid = info->si_pid;
        pid_t* arg = malloc(sizeof(pid_t));
        *arg = client_pid;

        pthread_t thread1;
        //printf("thread created\n");
        pthread_create(&thread1, NULL, client_thread, arg);
    }
    return;
}

int main(int argc, char** argv) {
    if (argc != 2) {
        printf("Incorrect input\n");
        return 0;
    }
    TIME_INTERVAL = strtol(argv[1], NULL, 10);
    pid_t server_pid = getpid();
    printf("Server PID: %d\n", server_pid);
    
    my_doc = markdown_init();
    my_clients = malloc(sizeof(struct all_clients));
    my_clients->num_clients = 0;
    my_clients->clients = NULL;

    //printf("doc and clients initialised\n");
    
    //char version_buf[64];
    //snprintf(version_buf, sizeof(version_buf), "VERSION %lu\n", my_doc->version/*version_number*/);
    version_log = calloc(1, sizeof(char));//malloc(strlen(version_buf)+1);
    //version_log[0] = '\0';
    //strcpy(version_log, version_buf);

    all_logs = calloc(1, sizeof(char));

    //printf("version log: %s || all logs: %s\n", version_log, all_logs);

    pthread_t broadcast_thread;
    pthread_create(&broadcast_thread, NULL, broadcast, NULL);

    sigset_t blocked_signals;
    sigemptyset(&blocked_signals);
    sigaddset(&blocked_signals, SIGRTMIN);
    sigaddset(&blocked_signals, SIGRTMIN+1);
    sigprocmask(SIG_BLOCK, &blocked_signals, NULL);

    struct sigaction sig1 = {0};
    sig1.sa_flags = SA_SIGINFO;
    sig1.sa_sigaction = sig_handler;
    sigaction(SIGRTMIN, &sig1, NULL);

    sigprocmask(SIG_UNBLOCK, &blocked_signals, NULL);

    //printf("signal setup done\n");

    char stdin_buffer[64];
    while (true) {
        if (fgets(stdin_buffer, sizeof(stdin_buffer), stdin)) {
            stdin_buffer[strcspn(stdin_buffer, "\n")] = '\0';
            if (strcmp(stdin_buffer, "DOC?") == 0) {
                pthread_mutex_lock(&doc_lock);
                char* doc_content = markdown_flatten(my_doc);
                pthread_mutex_unlock(&doc_lock);
                printf("%s\n", doc_content);
                free(doc_content);
            }
            else if (strcmp(stdin_buffer, "LOG?") == 0) {
                pthread_mutex_lock(&log_lock);
                printf("%s", all_logs);
                pthread_mutex_unlock(&log_lock);
            }
            else if (strcmp(stdin_buffer, "QUIT") == 0) {
                pthread_mutex_lock(&client_lock);
                if (my_clients->num_clients > 0) {
                    printf("QUIT rejected, %d clients still connected.\n", my_clients->num_clients);
                    pthread_mutex_unlock(&client_lock);
                    continue;
                }
                pthread_mutex_unlock(&client_lock);
                FILE* doc = fopen("doc.md", "w");
                pthread_mutex_lock(&doc_lock);
                markdown_print(my_doc, doc);
                pthread_mutex_unlock(&doc_lock);
                fclose(doc);
                break;
            }
        }
    }

    //handle eof
    //printf("terminating\n");
    pthread_mutex_lock(&term_lock);
    terminate = true;
    pthread_mutex_unlock(&term_lock);
    //printf("ending\n");

    //pthread_join(broadcast_thread, NULL);

    pthread_mutex_destroy(&doc_lock);
    pthread_mutex_destroy(&client_lock);
    pthread_mutex_destroy(&log_lock);
    pthread_mutex_destroy(&term_lock);

    free(version_log);
    version_log = NULL;
    free(all_logs);
    all_logs = NULL;
    free(my_clients);
    my_clients = NULL;
    markdown_free(my_doc);
    my_doc = NULL;
    return 0;
}